# ========================================
# FRONTEND ENVIRONMENT VARIABLES
# Copy this to .env.production for production build
# ========================================

# Backend API endpoint (must include /api path!)
# Dev: http://localhost:1230/api
# Prod: Your Render backend URL + /api
VITE_API_URL=http://localhost:1230/api

# ========================================
# VERCEL DEPLOYMENT
# ========================================
# When deploying to Vercel:
# 1. Go to Project Settings > Environment Variables
# 2. Add: VITE_API_URL = https://hannanagribusinessltd.onrender.com/api
# 3. IMPORTANT: Include /api at the end!
# 4. Make sure your backend CORS allows the Vercel domain
# 5. Vercel will auto-inject this during build
# 
# For local production testing: npm run build && npm run preview
